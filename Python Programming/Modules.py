# Module is a file that contains python code - var, functional, classes
import datetime
# benefits
# code reusability, easier maintainence
# better organization

# built in modules
# user defined modules

# built in modules - math, OS, sys, datetime, random, json, csv

import math
from datetime import datetime

print(math.sqrt(16))
print(math.pi)


print(datetime.now())
print(datetime.today())
print(date.today())
